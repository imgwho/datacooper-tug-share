# Datacooper TUG 分享讲稿

## 标题

从手工拖拽到工程化 BI：cwprep、cwtwb 和 datacooper.com

## 讲稿正文

各位好，今天我想分享的主题很简单：我们怎么把 Tableau 相关开发，从传统的手工点 UI，推进到可复现、可审查、可自动化的工程化流程。

我先说一个定位：Python 工具不是用来替代 BI 开发人员的，也不是要把分析师变成程序员，而是辅助加速 BI 开发过程。它更像一个加速器或者工程助手。

它做的事情主要有四类：

- 把重复性的工作自动化
- 把容易出错的步骤标准化
- 把已经验证过的做法沉淀成工具
- 让开发者把时间花在业务理解和结果判断上，而不是重复拖拽和复制粘贴

今天我会顺着这个思路，讲三个东西：

- cwprep，也就是 Tableau Prep 数据流生成工具
- cwtwb，也就是 Tableau Workbook 生成与工程化工具
- `datacooper.com`，也就是把这些能力产品化、在线化的入口

如果回头看 Tableau 的日常开发，最耗时的往往不是画出一个图，而是这些机械动作：

- 反复拖拽字段
- 反复调布局
- 反复复制 KPI 模块
- 反复迁移 workbook 到新数据源
- 反复检查文件能不能正常打开
- 反复修一些很细碎但很耗时间的问题

这些事情单个看都不难，但它们有一个共同问题：它们不创造新的业务价值，却持续消耗开发时间。

所以我做这些 Python 工具的时候，目标不是让 AI 变聪明，而是很务实地做一件事：把 Tableau 里那些高频、重复、规则明确的工作，变成可脚本化、可复用、可审查的流程。

换句话说：

- 复杂判断继续由人做
- 重复执行交给工具做
- 工程化工作交给代码做

先说第一个，cwprep。它本质上是一个 Tableau Prep 的文本生成引擎。

你可以把它理解成：输入一句话，输出一个可用的 Tableau Prep 数据流文件。

它主要解决的是 Tableau Prep 里的数据清洗和数据准备问题，比如：

- 连接数据库
- 读取 Excel / CSV
- join、union、filter
- pivot / unpivot
- 字段清洗
- 类型转换
- 计算字段
- 聚合
- 输出 `.tfl` 或 `.tflx`

它最关键的特点有四个。

第一，Text-to-PrepFlow。你不用先手工搭流，再去修细节，而是可以直接用自然语言描述数据处理逻辑，让工具生成 flow。

第二，MCP 集成。它可以接 Claude、Gemini、Cursor 这类支持 MCP 的客户端，也就是说，它不是一个只能在本地脚本里玩的工具，而是可以被 AI agent 调用的能力层。

第三，避免 GUI 依赖。Tableau Prep 当然很好用，但很多时候，GUI 里的拖拽并不适合批量化和自动化。这个工具的价值就是：你不用每次都打开界面，仍然可以构建、修改和导出数据流。

第四，可审查。它不只是生成，还支持把 TFL flow 翻译成 SQL。这个很重要，因为很多 BI 场景里，真正卡住的不是能不能做，而是能不能解释、能不能审、能不能给 DBA 看。

所以这类工具的核心价值，不是炫技，而是让数据准备这件事变得更工程化。

如果我用一句话总结它，就是：cwprep 不是替代 Tableau Prep，而是给 Tableau Prep 增加一层可以被 AI 和脚本驱动的工程入口。

第二个工具是 cwtwb，它做的事情和 cwprep 不一样。

如果说 cwprep 管的是数据怎么流动，那 cwtwb 管的是仪表板怎么生成。

它的定位不是聊天式分析助手，而是一个 workbook engineering layer。它的目标是让 Tableau workbook 变成一种可复现、可验证、可迁移的工程产物。

它主要解决的是 `.twb` 和 `.twbx` 这类 workbook 文件的生成、校验和迁移。

这意味着什么？

意味着你可以：

- 从代码或者 agent 调用生成 workbook
- 按规则搭建图表、布局和容器
- 校验文件结构，确保生成结果能正常打开
- 把现有 workbook 迁移到新的数据源
- 做一些重复性的 dashboard 工程操作，而不是每次重新手工搭

这个工具有几个很实用的特点。

第一，可复现。不是今天点出来一个样子，明天再点一遍又不一样。它强调的是确定性输出。

第二，可验证。它会做结构校验和 Tableau XSD 校验。所以你交付的不只是看起来差不多的文件，而是更接近生产级的文件。

第三，可迁移。这个很适合真实 BI 场景。很多时候不是重做一个 dashboard，而是把已有 dashboard 迁移到新数据源、新字段、新结构。

第四，支持复杂图表和布局编排。比如双轴图、KPI、map、layout、actions 等等。这意味着它不是只会生成一个简单 demo，而是能进入更接近真实项目的开发流程。

这里我会特别强调一句：cwtwb 不是为了让 AI 自己画图，而是为了把已经明确的 dashboard 设计规则，转成可以稳定复用的工程逻辑。

这点跟很多人想象中的“AI 画 Tableau”不太一样。它更像是把人的设计意图变成机器可以执行的结构化指令。

到这里可以把定位说清楚了。

这些 Python 工具，本质上不是 BI 的替代品，而是 BI 开发的加速器。

它们的定位有三个层次：

- 第一层，效率加速。把重复性工作自动化，比如生成 flow、复制 KPI、提取布局、生成 workbook
- 第二层，质量稳定。把常见规则固化下来，减少人工操作带来的偏差
- 第三层，工程协作。让 BI 不只是一个人点出来的文件，而是可以进入脚本、版本控制、团队协作和批量生产的流程

所以我不太想把它们说成 AI 神奇地替你做 BI，我更愿意说：Python 工具让 BI 开发从手工制作，往工程化和平台化走了一步。

接下来我想讲一下 `datacooper.com`。

如果说前面两个工具是能力本身，那 `datacooper.com` 就是把这些能力面向用户、面向社区、面向未来产品化的入口。

它未来不只是一个介绍项目的网站，而是想做成一个在线工具平台。

也就是说，长期方向不是只有本地脚本，而是：

- 能在线使用
- 能直接处理文件
- 能更低门槛地把工具开放给 BI 开发者
- 能把这些能力从开发者手里的代码变成可以直接点开用的工具

目前网站已经有两个很适合做成在线工具的原型：

第一个是布局解析工具。它能解析 Tableau workbook 的布局，把 dashboard 的结构提取出来。这个工具很适合做成“上传文件，自动解析布局，导出 JSON 或布局结果”的在线能力。

第二个是 KPI 复制工具。它能复制 KPI 工作表，并做局部替换。这个对很多 BI 场景特别实用，因为现实里经常不是从零开始做一个 KPI，而是复制一个已经验证过的模板，只替换其中一个指标，保留原来的结构和样式，避免 Replace References 那种全局误伤。

所以网站未来的方向我会概括成一句话：把 Tableau 的高频小工具，做成可在线访问的 BI 工具箱。

这其实和前面的两个 Python 工具是同一条路线，只是形态不同：

- Python 工具偏底层，适合开发者、脚本、自动化
- 网站工具偏应用层，适合更低门槛使用
- 两者最终都在服务同一个目标：让 BI 开发更快、更稳、更可复用

如果我要把今天的内容收束成一句话，我会说：

cwprep 负责 Tableau Prep 的数据流自动化，cwtwb 负责 Tableau Workbook 的工程化生成，而 datacooper.com 正在把这些能力做成更容易使用的在线工具平台。

更直白一点就是：

- 过去我们靠手工拖拽完成 Tableau 开发
- 现在我们可以用 Python 和 MCP 把重复劳动自动化
- 未来我们希望把这些能力进一步产品化、在线化、平台化

所以这不是单纯在讲两个工具，而是在讲一个方向：让 Tableau 开发从手艺活，变成工程化工作流。

最后我想留一句话：

我们不是要让人离开 Tableau，而是要让人把时间花在真正重要的地方。

谢谢大家。
